info.bat :
simply open a console (Win+R --> then type 'cmd') and type info.bat 
the script creates a text (.txt) file name as the local host machine
e.g. if your local host name is MAX_PC then the infos are written 
in the same directory of info.bat under the name MAX_PC.txt

